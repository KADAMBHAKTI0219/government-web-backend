import Nomination from './Nomination.js';

// Application model is maintained as an alias to Nomination to preserve backward compatibility across services
const Application = Nomination;

export default Application;
